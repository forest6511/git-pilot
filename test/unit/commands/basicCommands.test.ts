/**
 * Basic commands tests
 */

describe('BasicCommands', () => {
  describe('refresh', () => {
    it('should refresh git status', () => {
      expect(true).toBe(true);
    });
  });

  describe('openRepository', () => {
    it('should open repository', () => {
      expect(true).toBe(true);
    });
  });
});